1、同步(SYNC)和异步(ASYNC)编程
2、JS是属于单线程的，我们最好使用异步编程
	定时器都是异步编程的
	事件绑定都是异步编程的
	回调函数也可以是异步编程的
	在AJAX中可以使用异步编程

3、ajax中的同步编程指的是：当ajax任务开始的时候，一直到readyState === 4 的时候任务才结束。此时才可以处理其他的事情

4、ajax中的异步编程指的是：当ajax任务开始的时候，不需要等到readyState === 4 ，我依然可以继续处理其他的事情。然后再返回看readyState是否到4，到达4的时候我们在做一些相关的操作