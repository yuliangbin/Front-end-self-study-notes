1、什么叫函数表达式
	把函数定义的部分当作一个值赋值给我们的变量，或者元素的某一个点击事件
	
2、自执行函数在全局作用域下不进行预解释
	因为自执行函数是定义后马上执行的，所以不参与预解释
	
3、函数体中的return下面的代码虽然不执行，但是仍会进行预解释。return后跟着的返回值，不进行预解释
	function(){
		console.log(num);
		return function(){};
		var num = 2;
	}